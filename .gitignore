# ---- Python ----
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib60/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
.venv/
venv/
env/
ENV/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
*.cover

# ---- Node ----
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.pnpm-store/
dist/
dist-ssr/
*.local
.vite/
coverage/

# ---- Env / secrets ----
.env
.env.*
!.env.example
*.pem
*.key
!**/testdata/**/*.key

# ---- Editors / OS ----
.vscode/*
!.vscode/extensions.json
!.vscode/settings.example.json
.idea/
.DS_Store
Thumbs.db
*.swp

# ---- Local data volumes ----
.data/
storage-local/
tmp/
scratch/

# ---- Build artifacts ----
*.log
*.tsbuildinfo
frontend/stats.html
# `make openapi` writes this at the repo root; it is generated from the running
# API, so a committed copy is stale the moment a route changes.
openapi.json

# ---- Deployment ----
# scripts/deploy-remote.sh packages the tree into a tarball and unpacks it on the
# target as deploy.tar.gz. Neither belongs in the repository, and the archive is
# large enough to be worth never committing by accident.
deploy.tar.gz
clear-deploy-*.tar.gz
# scripts/deploy.sh moves the development-only compose override aside on a server.
# Covered by `*.local` above, named here so the reason is discoverable.
docker-compose.override.yml.local
